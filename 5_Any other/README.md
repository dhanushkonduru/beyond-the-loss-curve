# Any Other

Supporting material that doesn't fit the other five categories: the measured results behind every number in the manuscript, and the manuscript's markdown source.

## `results_and_metrics/`

Every measured result the paper reports, generated by the pipeline in [`../2_Source code/pipeline/`](../2_Source%20code/pipeline/).

| File | Content |
|---|---|
| `seeds_summary.txt` / `.json` | The headline table — UES, KRR, TS, BCM, PLI, ARRM, mean ± sd over three seeds |
| `audit_report_*.txt` | Per-method behavioural audit reports (Gradient Ascent, Gradient Difference, NPO) |
| `audit_matrix_*.json` | The raw per-record leak vectors and verdicts behind each audit report |
| `mia_report.txt` / `mia.json` | The membership inference attack (Section VIII-B) |
| `relearn_report.txt` / `relearn.json` | The relearning attack (Section VIII-C) — the paper's central result |
| `comparison.txt` | Every checkpoint compared against the retrained reference model |
| `sync_report.txt` / `sync.json` | The live WHO synchronisation cycle (Section VII-F, Table 5) |
| `sequential_report.txt` | Sustainability under repeated deletion rounds (Section VIII-D) |
| `sweep_report.txt` / `sweep.json` | Operating-point sweep across unlearning strengths |
| `*_log.txt` | Full console logs from each pipeline stage |
| `seeds/` | Raw per-seed audit matrices and attack outputs (seeds 42, 43, 44) — the source data behind `seeds_summary.txt` |
| `extra_figures/` | Analysis figures produced during the study but not included in the manuscript |

## `paper_source/`

`paper_full.md` — the markdown source the manuscript is built from. Rebuilding the `.docx` from this file:

```bash
pandoc paper_full.md -o paper_raw.docx --reference-doc="<IEEE Access template>.docx"
python apply_template_format.py paper_raw.docx Beyond_the_Loss_Curve_Manuscript.docx
```

(`apply_template_format.py` is in [`../2_Source code/paper_figures/`](../2_Source%20code/paper_figures/).)
